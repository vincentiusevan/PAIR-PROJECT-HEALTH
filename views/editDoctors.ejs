<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Doctors</title>
  </head>
  <body>
    <main>
      <div class="title">
        <h1>Edit Doctors</h1>
      </div>
      <form action="/doctors/<%= doctors.id %>/editDoctors" method="post">
        <div class="column">
            <label for="">Image URL</label>
            <input type="text" name="imageUrl" value="<%= doctors.imageUrl %>">
        </div>
        <div class="column">
          <label for="">Name Doctor</label>
          <input type="text" name="name" value="<%= doctors.name %>"/>
        </div>
        <div class="column">
          <label for="">Doctor specialist</label>
          <% let specialization = ["THT", "Mata", "Bedah", "Penyakit dalam","Paru","Otak"] %>
          <select name="specialization" id="" value="specialization">
            <% specialization.forEach(specialization => { %>
             <option value="<%= specialization %>"><%= specialization %></option>
            <% }) %>
          </select>
        </div>
        <div class="column">
          <label for="">Degree</label>
          <% let degree = ["S1", "S2", "S3"] %>
          <select name="degree" id="" value="degree">
            <% degree.forEach(degree => { %>
             <option value="<%= degree %>"><%= degree %></option>
            <% }) %>
          </select>
        </div>
        <div class="column">
            <button type="submit">Edit</button>
        </div>
      </form>
    </main>
  </body>
</html>
